#ifndef _HOST_FLAGS_H
#define _HOST_FLAGS_H

/*
 * Define this macro if the code is to be compiled for older versions of OpenSSL
 * that lack e.g. EVP_MD_CTX_new, otherwise leave it undefined
 */
#undef OLD_SSL

#endif
